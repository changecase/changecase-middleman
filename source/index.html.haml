---
title: Jeff Jacobson-Swartfager
alias:
  - rss.xml
change_frequency: weekly
---

#featured
  // Put featured content (slideshow?) here

%ul.feat
  %li
    = link_to 'Blog', '/blog',  class: "feat-icon blog"
  %li
    = link_to 'Work', '/work',  class: "feat-icon work"
  %li
    = link_to 'About', '/about', class: "feat-icon about"

-#
  %header
    %h1 Recent Blog Posts
  - blog('blog').articles[0...5].each do |article|
    %section
      %h2 
        %a{ href: article.url }= article.title
        %br
        %time= article.date.strftime('%b %e %Y')
      = article.summary
      %a{ href: article.url } Read more

  %header
    %h1 New Work
  - blog('work').articles[0...5].each do |article|
    %section
      %h2 
        %a{ href: article.url }= article.title
      = article.summary
      %a{ href: article.url } Read more
