describe("Collapsable Main Navigation", function() {

  it("hides the main naviation on small screens", function() {});
  it("replaces the expanded navigation with a hamburger on small screens", function() {});
  describe("when expanded,", function() {
    it("expands the main navigation when the hamburger is clicked", function() {});
  });
  describe("when collapsed,", function() {
    it("collapses the main navigation when the hamburger is clicked", function() {});
  });
});
